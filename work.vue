 // --- 🔥 ФИКС ЗНАЧЕНИЙ ---
    var v = getFormField(oField.name, "");
    if (v != "")
        oField.value = v;   // если пользователь реально ввёл что-то — сохранить
    // иначе оставить дефолт (oField.value)
